Drop store/push service-account JSON keys here.
This whole directory is gitignored.

Expected files:
  google-play-service-account.json  -> Play Console submissions (eas submit -p android)
  fcm-service-account.json          -> Android push (upload via `eas credentials`, then delete)
